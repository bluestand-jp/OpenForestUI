> 🌐 **English** ・ [日本語](README-ja.md)

# Broadcast skin icons

Objective icons for the broadcast skin, rendered on the comparison bottom bar / scoreboard (`PrmBottomBarVisual`, `PrmScoreboardVisual` in [`../../../src/visual`](../../../src/visual)). Preloaded as `prm_*` in [`../../../src/scenes/IngameScene.ts`](../../../src/scenes/IngameScene.ts).

## Contents

- `dragon.png` (`prm_dragon`), `baron.png` (`prm_baron`), `grub.png` (`prm_grub`), `tower.png` (`prm_tower`), `gold.png` (`prm_gold`) — objective-counter icons for the broadcast top/bottom bar.
- `flags/` — currently empty (reserved for per-team/nation flag art).
